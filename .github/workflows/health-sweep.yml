name: health-sweep

on:
  schedule:
    # Monthly, 1st at 16:30 UTC (8:30/9:30 am Pacific)
    - cron: "30 16 1 * *"
  workflow_dispatch:

jobs:
  sweep:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 50

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install Claude Code
        run: npm install -g @anthropic-ai/claude-code

      - name: Run the sweep
        env:
          # Subscription auth: long-lived token from `claude setup-token`,
          # stored as a repo secret. No API key; usage bills to the Max plan.
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
        run: |
          claude -p "$(cat .github/sweep-prompt.md)" \
            --model claude-fable-5 \
            --allowedTools "Bash,Read,Write,Edit,Glob,Grep,WebFetch,WebSearch" \
            --permission-mode acceptEdits \
            --verbose
